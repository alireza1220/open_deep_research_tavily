
let's add this or something similar to this to the open_deep_research/src/open_deep_research/utils.py

def get_base_url_for_model(model_name: str, config: RunnableConfig):
    """Get API key for a specific model from environment or config."""
    should_get_from_config = os.getenv("GET_API_BASE_URL_FROM_CONFIG", "false")
    model_name = model_name.lower()
    if should_get_from_config.lower() = "true":
        api_base_url = config.get("configurable", {}).get("apiBaseUrl", {})
        if not api_base_url:
            return None
        if model_name.startswith("openai:"):
            return api_base_url.get("OPENAI_API_BASE_URL")


Also create a sample python file to test that uses the configuration instead of .env. also create a curl command.


