


Notes on both endpoints stream and is not populated.

class StreamEvent(BaseModel):
    """Server-Sent Events format for streaming responses."""

    event: str = Field(description="Event type (e.g., 'progress', 'complete', 'error')")
    data: Dict[str, Any] = Field(description="Event data payload")


I would like a unified payload for the data

class Citation(BaseModel):
    link:
    ...


class EventData(BaseModel):
    final_report: str
    final_resport_without_citations: str
    citation: List[Citation]
    messages: 
    notes: list[str]
    research_brief: str
    nodes: list[str]
    progress_message: str (example: completed briefing now working on supervisor agent or all completed drafting the final version)
    has_final_report: boolean
    started_time:
    ended_time:
    duration:
    visited_websites: accumulated from the number of citations that are returned by Tavily or other search engines
    searches_occured:
    configuration: (the configuration used for this call)
    error_message:
    stats: Any useful statistics for this.

class StreamEvent(BaseModel):
    """Server-Sent Events format for streaming responses fixed format."""

    event: str = Field(description="Event type (e.g., 'progress', 'complete', 'error')")
    data: EventData

at teh moment sometimes has something and sometimes doesn't like the following 
{"final_report":" .... ","notes":{"type":"override","value":[]},"research_brief":"...."}
{"nodes":["research_supervisor"],"has_final_report":false}



data: {"event":"progress","data":{"nodes":["clarify_with_user"],"has_final_report":false}}
data: {"event":"progress","data":{"nodes":["write_research_brief"],"has_final_report":false}}
data: {"event":"progress","data":{"nodes":["research_supervisor"],"has_final_report":false}}
data: {"event":"complete","data":{"final_report":"# The World's Most Visited Websites .... ",\n
                                "messages":[{"role":"assistant","content":"# The World's www.statista.com/topics/12081/top-websites-worldwide/"}],
                                "notes":{"type":"override","value":[]},
                                "research_brief":"I want to k I ha....."}}

data: {"event":"complete","data":{"final_report":"# The World's Most Visited Websites .... ,\n
                                "messages":[{"role":"assistant","content":"#  Statista:081/top-websites-worldwide/"}],\n
                                "notes":{"type":"override","value":[]},\n
                                "research_brief":"I wdted .... ."}}






