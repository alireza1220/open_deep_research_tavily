
I would like to ensure the following would work fine:
openwebui/deep_search_tool_to_be_complete.py line around 236 to line 280 and line around 59 to 68

I would like to also work on this filter:
openwebui/filter/privacy_filter_with_namev5.py line 283 to 286


I would like also update this basically it should in open_deep_research/openwebui/filter/privacy_filter_with_namev5.py


    ## TODO updating the async def stream to pause the streaming if there is deep search word in the tool, basically if the tool got the deep search call there should not be a post call to it.
    # async def stream(self, body)
    #     async def stream(
    #     self,
    #     event: dict
    # ) -> dict:
    # This is where you modify streamed chunks of model output.
    # print(f"stream event: {event}")
    # print(f"self event: {self}")
    # print()
    # if self.is_deepsearch_called True inlcude deepsearch or deep_search (lower case) just don't return anything return event()
    # return event choise to be empty like this or just empty
    # update_event = {
    #     "id": "chatcmpl-b43616451a5944a7b4380b4925a812c4",
    #     "created": 1767137139,
    #     "model": "Llama4-Scout",
    #     "object": "chat.completion.chunk",
    #     "choices": [{"index": 0, "delta": {"content": ""}}],
    # }
    # update event should be empty
    # return updated_event


also analyzing this in the tool: openwebui/deep_search_tool_to_be_complete.py 
        # TODO analyze the self here and see if the stream object is in the self here that is passed or is it possible to pass the self from the inlet to here so we use that instead of the deep search
        # print(f"self in tool is {self}")



